window.onload = () => {
    // TweenMax.to("#rect", 3, { left: 500 });
    let video = document.querySelector("#site-video")
    if (video) {
        video.play()
            // console.log(video)
    }

}